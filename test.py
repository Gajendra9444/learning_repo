print("no worries")

# git clone "repo-url" -- in ssh
# open the git repo folder in s code 
# changes are done in the repo folder 
# after we done with changes -- git add or git add files
# after add -- git commit -m "relavent message"
# noe update changes in remote
# -- git push origin main  